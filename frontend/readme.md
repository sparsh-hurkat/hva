write the frontend architecture based on the apis we designed for the backend. keep it simple, react, typescript. readable and maintainable
1st page - release work items list with a structured count of issues, by each tool, date and an AI summary of the counts of the issues and their details. the backend API can be updated if we want. just mock the backend api structure you recommend to generate the frontend. and give me a flag to switch between mock and actual apis.
when you click on a release work item - get the release work item details with strucured data as well as an ai summary on each build.
click on a build id to get more details with an ai summary.
basically this whole should be readable by a non technical person as well.